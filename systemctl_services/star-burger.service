# Содержимое файла /etc/systemd/system/star-burger.service
[Unit]
Description=star burger guniron runner
Requires=postgresql.service

[Service]
WorkingDirectory="CHANGE ME TO YOUR PATH"
ExecStart=/usr/local/bin/poetry run python -m gunicorn -b 127.0.0.1:8080 star_burger.wsgi:application
Restart=on-failure
RestartSec=5

[Install]
WantedBy=multi-user.target
